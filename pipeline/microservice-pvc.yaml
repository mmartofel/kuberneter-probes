apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: microservice-pvc
  namespace: project01
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 2Gi

